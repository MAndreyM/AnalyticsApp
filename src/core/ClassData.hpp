// ==============================
// ФАЙЛ: src/core/ClassData.hpp
// ==============================
#pragma once

#include "Student.hpp"
#include <string>
#include <vector>
#include <memory>
#include <algorithm>
#include <stdexcept>

/**
 * @struct ClassData
 * @brief Структура данных класса (оптимизированная версия)
 * 
 * Содержит информацию о классе: название, список учеников,
 * предметы и результаты анализа по категориям.
 * 
 * Использует shared_ptr для избежания копирования учеников
 * и обеспечения согласованности данных. Все ученики хранятся
 * централизованно, а результаты анализа ссылаются на них через индексы.
 */
struct ClassData {
    // ==============================
    // Основные поля класса
    // ==============================
    
    std::string className;          ///< Название класса (например, "10А")
    int gradeNumber;                ///< Номер параллели (например, 10)
    std::string classLetter;        ///< Буква класса (например, "А")
    
    /**
     * @brief Централизованное хранение учеников через умные указатели
     * 
     * Использование shared_ptr позволяет:
     * 1. Избежать копирования учеников при передаче
     * 2. Обеспечить согласованность данных
     * 3. Автоматически управлять памятью
     */
    std::vector<std::shared_ptr<Student>> students;
    
    // ==============================
    // Результаты анализа - храним ИНДЕКСЫ учеников
    // ==============================
    
    // ==============================
    // Базовые методы работы с классом
    // ==============================

    /**
     * @brief Конструктор по умолчанию
     */
    ClassData() : gradeNumber(0) {}

    /**
     * @brief Конструктор с параметрами
     * @param name Название класса
     * @param grade Номер параллели
     * @param letter Буква класса
     */
    ClassData(const std::string& name, int grade, const std::string& letter)
        : className(name), gradeNumber(grade), classLetter(letter) {}

    /**
     * @brief Добавляет ученика в класс (через shared_ptr)
     * @param student Умный указатель на ученика
     */
    void addStudent(std::shared_ptr<Student> student) {
        if (student) {
            students.push_back(student);
        }
    }

    // ==============================
    // Методы для работы с предметами
    // ==============================


    // ==============================
    // Методы управления результатами анализа
    // ==============================

    // ==============================
    // Методы для получения учеников по категориям
    // ==============================

    // ==============================
    // Методы для заполнения результатов анализа
    // ==============================

    // ==============================
    // Вспомогательные методы
    // ==============================

    // ==============================
    // Операторы сравнения
    // ==============================

};