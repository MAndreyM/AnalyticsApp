// ============================================
// ТЕСТ: test_grade_calculator.cpp
// Назначение: Тестирование калькулятора оценок
// Статус: TDD этап 0 - первый тест
// ============================================

#include "doctest.h"
#include "services/GradeCalculator.hpp"

TEST_CASE("GradeCalculator: базовый тест конвертации") {
    SUBCASE("Тест на минимальную функциональность") {
        // Первый тест TDD: проверяем, что метод существует и работает
        int result = GradeCalculator::convertScoreToGrade(3.0);
        
        // На этапе 0 проверяем только, что метод вызывается и возвращает значение
        // Используем переменную, чтобы избежать предупреждения
        (void)result; // Явное указание, что переменная может не использоваться
        
        // TODO: В следующих этапах добавим проверки значений
        // CHECK(result == 3);
    }
    
    SUBCASE("Тест нулевой оценки") {
        int result = GradeCalculator::convertScoreToGrade(0.0);
        (void)result; // Используем переменную
    }
}